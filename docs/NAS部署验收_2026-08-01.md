# 京奥智库 NAS 部署验收记录（2026-08-01）

## 结论

京奥智库 v1.12 已在飞牛 NAS 完成持久化知识进化版本升级，局域网运行验收通过。升级保留原 `.env`、PostgreSQL 数据卷、备份和合成测试资料，没有开启任何模型出网能力，也没有导入或发布真实资料。

公网 FRP 控制连接仍超时，因此本记录只证明局域网技术试点可用，不证明 `https://knowledge.jingao.club/` 已经可用。

## 版本

- 当前源码版本：`jingao-copilot-nas-20260801-162046`；
- 运行目录继续使用原 `jingao-runtime`，环境变量和数据卷原样保留；
- 无密钥部署包：`jingao-copilot-nas-20260801-162046.zip`；
- 部署包 SHA-256：`617AAF2FC9327AC738FC00E65D30F1ECE42247E5710DF2F19908D8C2C3E5B851`；
- `CURRENT-SOURCE.txt` 与部署说明均已同步到 v1.12。

维护结束后，NAS `/tmp` 上传包、运行探针和中间结果均已移除，容器内临时探针也已删除。fnOS SSH 已重新关闭，22 端口复核不可达。

## 备份门槛

- 升级前备份：`jingao-20260801T083652Z-0f1ef7fe`；
- 升级后备份：`jingao-20260801T083947Z-7ea4a138`；
- 两次备份均通过清单与文件 SHA-256 校验；
- 两次备份的可用原件缺失数均为 0；
- 升级后备份清单包含 `maintained_artifacts`、`evolution_review_runs`、`evolution_candidate_records`、`evolution_change_plans`，当前计数均为 0。

## 运行验收

- PostgreSQL、Agent、Web、ingest、reconcile、backup、Uptime Kuma 共 7 个服务运行；
- 7 个服务在本轮验收时均无异常重启；
- Agent、聚合监控、员工 Web、Uptime Kuma 探针全部通过；
- NAS Docker 内 Web 生产构建通过；
- PostgreSQL 四张知识进化新表均存在；
- 创始人真实会话登录返回 200，`/v1/me` 验证角色为 `founder`；
- 维护资料列表与候选基线接口返回 200，当前数量均为 0；
- 手工执行 `evolution-due` 返回 0 个到期、0 个新任务，且 `automatic_publication=false`；
- 四个模型出网开关保持 `false`；
- Agent 全量自动化为 76/76。

## 知识进化安全口径

- 维护资料只能登记已经发布、当前、定稿且原件完整的基线；
- 定时任务只创建到期复核，不生成成稿、不发布文档；
- 候选、披露批准、变更计划、复核关闭、文档发布和基线更新是六个独立动作；
- 冲突不能通过人工批准绕过；
- 决策和计划生成前重新核验来源状态及哈希；
- 关闭复核不会推进资料截止线；
- 更新基线只接受已经发布为当前版、直接替代旧基线的新文档；
- 全流程记录审计，但审计日志不保存证据正文、令牌或 NAS 绝对路径。

## 公网状态

FRPC v0.69.0 的 `jingao-frpc.service` 保持运行并自动重试，累计重启次数表明控制连接长期未建立。本轮复核中，NAS 到 `82.156.239.212:7001` 仍连接超时，而维护电脑可以连接公网服务器 7001/8000。

进一步对照证明：NAS 当前公网出口为 `1.202.100.44`，与维护电脑出口不同；NAS 可以成功连接公开测试站的 TCP 7001。因此 NAS 侧没有普遍封禁出站 7001，剩余阻塞点是腾讯云安全组、服务器主机防火墙、拒绝列表或上游对 NAS 新出口的源地址过滤。

下一步应放行 `1.202.100.44/32 → TCP 7001`，不要长期开放 `0.0.0.0/0`。HTTPS 反向代理完成后还必须删除 8000 的公网入站规则，只允许服务器本机访问 `127.0.0.1:8000`，并轮换调试期 FRP 令牌。完成这些动作和外网登录验收前，不开放员工公网登录，也不把公网入口记为完成。
