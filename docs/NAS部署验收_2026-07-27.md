# 京奥智库 NAS 部署验收记录（2026-07-27）

## 结论

京奥智库首版已部署到飞牛 NAS，可在公司局域网内进入登录页并使用本地确定性检索。
本次是 5 人试点技术上线，不等于真实资料、员工权限或正式发布验收。

## 入口

- 推荐入口：`http://jadj-nas.local:3000/`
- 当前局域网地址：`http://192.168.2.53:3000/`
- 当前地址由 DHCP 分配，若路由器后续变更地址，优先使用主机名入口；
  正式试点前建议在路由器中为 NAS 设置 DHCP 地址保留。
- 只开放 Web 的 3000 端口到局域网；数据库与 Agent 不映射宿主机端口。

## 部署位置

- 版本源目录：
  `/vol1/@team/京奥智库/00_部署/jingao-copilot-nas-20260727-142832`
- 运行目录：`/vol1/@team/京奥智库/00_部署/jingao-runtime`
- 知识资料目录：`/vol1/@team/京奥智库/01_知识资料`
- 待审核目录：
  `/vol1/@team/京奥智库/01_知识资料/99_AI入库待审核`
- 备份目录：`/vol1/@team/京奥智库/02_备份`

运行目录的 `.env` 为 root 专用权限，不进入部署包、Git 或共享资料目录。

## 服务验收

| 服务 | 结果 |
|---|---|
| PostgreSQL 16 + pgvector | 健康 |
| Agent API | 健康，仅 Docker 内网 |
| Web | 健康，局域网 3000 端口 |
| 待审核扫描 ingest | 首轮成功 |
| 原件对账 reconcile | 首轮成功 |
| 每日备份 backup | 当前快照成功，数据库与原件均校验 |
| Uptime Kuma 2.3.2 | 健康，局域网 3001 端口 |

全部容器重启次数为 0。实测运行内存合计约 365MiB，远低于 16GB
设备容量；数据盘使用约 1%。

## 数据与检索验收

本次只使用两份内容完全相同、明确标注为“非真实业务数据”的合成 TXT：

- 发现文件：2
- 新增文档：1
- 同哈希重复：1
- 解析失败：0
- 不支持类型：0
- 原件哈希异常：0

Web 登录、工作台、系统状态和本地查询均通过。查询可检索到合成资料，
并明确标注为候选内容，不把它当作公司当前事实。

## 测试基线

- 后端自动化回归：60/60 通过。
- 5 人并发：50 次请求，0 错误，权限数量侧信道 0，
  P95 约 15.8ms。
- 当前语料可生成技术探针 21 项：页码引用 100%、拒答 100%、
  权限泄露 0、本地检索 P95 约 1.8ms。
- 业务金标：0/150。真实语料尚未获准入库，因此不生成虚假业务题。

## 备份恢复演练

- 最新完整备份：
  `jingao-20260727T085014Z-3cb77697`
- 备份清单已封口，数据库归档可读取；
- 1 份合成原件已按 SHA-256 封入备份，
  `source_files_missing_at_backup=0`；
- 在独立临时 PostgreSQL 容器和独立临时数据卷中恢复，不连接、不覆盖
  运行库；
- 用户、项目、文档、分块数量与运行库一致；
- 引用源文件存在且哈希一致；
- 恢复库旧会话已注销；
- 临时容器和临时卷已删除。

演练过程中发现并修复两项问题：首次备份早于测试资料入库；备份容器此前
没有封入原件路径。修复后增加回归测试并重新完成隔离恢复，旧的不完整或
过早快照不作为恢复基线。

## 监控验收

- `京奥智库运行状态`：每 60 秒检查 Agent `/monitorz`，覆盖数据库、
  存储、内存和备份；
- `京奥智库员工入口`：每 60 秒检查 Web 首页；
- 两个监控均设置 2 次重试、10 秒超时，当前均为正常；
- Uptime Kuma 重启后两个监控和历史心跳仍在；
- 没有挂载 Docker Socket；
- 飞书/微信外部告警目标未获确认，因此尚未启用消息推送。

## 安全边界

- `JINGAO_LLM_ENABLED=false`
- `JINGAO_LLM_L3_ENABLED=false`
- `JINGAO_EMBEDDING_ENABLED=false`
- `JINGAO_EMBEDDING_L3_ENABLED=false`
- NAS 未注入模型网关 Key。
- L4 永不进入云端向量或生成上下文。
- 真实资料未导入，真实员工账号和权限未分配，候选资料未发布为
  `current`。
- SSH 只在本次部署窗口临时开启，完成后关闭；后续维护按 Runbook
  临时开启并在任务结束后关闭。
- 收尾实测 NAS 的 22 端口已关闭，Web 仍返回 200；临时部署公钥已从
  NAS 与本机移除。
- Uptime Kuma 仅开放在局域网 `3001`，未通过 FN Connect 或路由器做
  额外端口映射。

## 已知限制

- 当前只有一块 8TB 数据盘，无 UPS；适合试点，不具备磁盘冗余。
- 当前备份目录与数据库在同一块盘，只能应对逻辑错误，不能应对硬盘损坏。
- 未配置飞书 OAuth；试点仍为本地账号。
- 未启用云模型生成、Embedding、自我进化、图片语义检索和视频索引。
- 真实业务的 150 道金标题、原页抽检和权限越界题需在首批资料与账号
  获批后完成。

## 下一道授权门

在导入真实公司资料前，创始人需确认：

1. 首批资料清单及真实性；
2. 每份资料的密级、定稿和当前/历史口径；
3. 5 名员工的账号、角色和部门范围；
4. 是否启用 L1/L2 的模型生成与 Embedding；
5. 哪些候选资料可以发布为当前版本。

最终可重复部署包会随交付给出文件名与 SHA-256。安全扫描必须保持：
0 个禁入文件、0 个已知密钥或密码命中，且不包含 `.env`、数据库、
知识原件、SSH 文件或云托管配置。
