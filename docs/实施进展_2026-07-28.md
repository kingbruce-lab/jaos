# 京奥智库实施进展（2026-07-28）

## 当前结论

NAS 局域网首版已经形成可重复部署、可备份恢复、可监控、可受控发布的技术
闭环。本轮补齐公网域名下的同源 API 链路、Web 入口防护和混合 PDF 逐页
OCR，并在 NAS 完成第三轮增量升级和全套运行验收。

公网访问尚差服务器侧网络放行与 HTTPS 反向代理；真实资料、真实员工权限和
云端模型开关仍等待创始人分别确认。

## 本轮完成

- Web 登录、问答、预览、下载统一走 `/api/kb` 同源代理，公网域名下不依赖
  `192.168.*` 地址；
- 增加精确 Host 白名单，非法 Host 返回 421；
- 增加 CSP、禁止嵌入、来源策略、权限策略、内容类型保护和 HTTPS HSTS；
- 增加 2 MiB 请求体上限、分块请求计数、逐跳头清理和上游超时；
- 修复 Docker 内部监控使用 `Host: web` 时被白名单误拦截的问题；
- 更新首次部署脚本：从脚本位置自动识别版本目录，升级时保留既有 `.env`，
  不再重置数据库密码；
- 重新生成无密钥部署包并在 NAS 完成 Web 容器重建；
- 升级前备份通过，升级后 7 个服务和完整运行验收全部通过；
- PDF 从“整份无文字才 OCR”改为逐页判断：只 OCR 低文字页，保留并合并
  其他页面的原生文字，物理页码不变；
- 待审核目录每次扫描后自动处理新派生任务，已完成任务不重复运行，失败任务
  阻断发布并等待人工排查；
- FRPC v0.69.0 保持 active 并自动重连。

## 自动化与实测

- Agent：62/62 通过；
- 无文字层 PDF 与“原生文字页 + 扫描图片页”的混合 PDF 均通过真实 OCR；
- 混合 PDF 两页已逐页渲染核对，页面顺序、尺寸和引用页码一致；
- Web 代码检查：通过；
- Web 生产构建与渲染测试：通过；
- Web 安全单元测试：4/4 通过；
- 本地同源真实链路：登录、`/v1/me`、搜索全部返回 200；
- NAS 合法公网 Host：200；
- NAS 非法 Host：421；
- NAS 全套验收：PASS；
- 升级前完整备份：`jingao-20260728T140249Z-2d68e81e`；
- 最新运行验收备份：`jingao-20260728T140346Z-f4664258`；
- 最新部署包：
  `jingao-copilot-nas-20260728-215729.zip`，
  SHA-256 为
  `F9E0DD6BCA8A145F231F32EE66A77A220779A9EB119973210C12FD098ECAA556`。

## 仍需外部动作

NAS 对外部诊断主机的 7001/8000 均可连接，但只对目标服务器
`82.156.239.212` 的 7001/8000 超时，阻断已定位到目标服务器安全组、
主机防火墙或来源白名单。公网服务器需要允许 NAS 当前出口
`106.37.113.25/32` 访问 TCP 7001，并在 443 HTTPS 入口把
`knowledge.jingao.club` 反向代理到
`127.0.0.1:8000`。完成后即可进行公网登录、搜索、原页预览和 PPTX 下载
终验。

## 仍需创始人确认

1. 首批真实资料及每份资料的真实性、密级、定稿与 current/历史口径；
2. 5 名员工的登录名、显示名、角色、部门和密级上限；
3. 是否先只开启 L1-L2 LLM，Embedding 暂缓；
4. NAS 外第二备份目标；
5. 飞书/微信告警接收目标。

## 待重新提供的测试文件

此前上传的现行公司介绍 PDF 已不在原路径。若要完成真实版面、中文 OCR、
页码引用和公司事实抽检，需要重新上传该 PDF；重新获得文件不等于授权正式
入库或发布，仍按上面的资料真实性与密级确认关口执行。
