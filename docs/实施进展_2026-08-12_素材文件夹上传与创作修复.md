# 京奥智库实施进展：素材文件夹上传与智能创作修复

日期：2026-08-12

## 本次上线

- 新增图文、音频、视频素材的整文件夹上传入口；文件夹名作为整批素材的项目名称，内部相机文件名无需逐个修改。
- NAS 保存结构固定为 `资料分类 / L1-L5 / 文件夹名 / 原子目录 / 原文件名`，最多选择 500 份受支持文件。
- 文件夹和内部相对路径在服务端做路径越界校验；完全重复内容继续按 SHA-256 自动过滤。
- 智能创作公网 FRP HTTP 超时已提高到 300 秒；应用不再对主、备模型超时执行第二轮完整重试，生成上限由 3600 tokens 收敛为 2600 tokens。
- 生产环境真实模型验证通过：`grok-4.5` 返回有效标题与 `[S1]` 引用。

## 数据清理

- 已从数据库与 NAS 原件目录删除 `JAG影像资料` 分类内 140 份无业务命名素材，共约 3.19 GB。
- 删除范围为 120 份相机编号图片、17 份相机编号视频和 3 份哈希命名素材；数据库文档、文本块、派生 OCR、源文件记录同步清理。
- 分类本身及其 L1-L5 空目录保留，便于后续使用已命名文件夹重新上传。

## 验证

- 后端上传、生成、写作和检索相关自动化测试 36 项通过。
- 前端 ESLint 与正式构建通过。
- NAS 容器全部健康，公网 `https://knowledge.jingao.club/api/kb/healthz` 返回 `ok`。
- 当前部署包：`jingao-copilot-nas-20260812-110700.zip`。
- SHA-256：`B55E724955C58EEFB5B60EE3179C1B0BBA5A28FEE254A5359C1CE373A0874576`。
