# 京奥智库实施进展（2026-07-26）

## 本轮完成

| 对应任务 | 当前证据 | 状态 |
|---|---|---|
| P2-01 数据模型 | 已实现 User / Session / Project / FileBlob / Document / Chunk / ReviewProposal / AuditLog；文件哈希、文档语义与审核建议分离 | 本地版完成 |
| P2-02/P2-03 解析 | PDF、PPTX、DOCX、XLSX解析器和分块已实现 | 本地版完成 |
| P2-05 入库 | JSON清单导入、哈希去重、失败隔离；资料管理员提交元数据建议、创始人确认应用/退回，确认后仍保持 candidate | 本地治理闭环完成，NAS 自动移动待实机 |
| P3-01 检索 | 检索前密级过滤、当前/历史自动分类、本地全文评分 | 第一层完成 |
| P3-02 引用与拒答 | 返回文件、项目、版本状态、页码；无current证据时回答“资料中未找到” | 第一层完成 |
| P3-03 API | 登录、状态、检索、项目、文档、审核队列、审核建议/应用/退回、提案骨架、可编辑 PPTX 下载；OpenAPI可用 | 本地版完成 |
| P3-04 Web | 登录、工作台、项目库、带出处检索、提案共创、可编辑 PPTX 下载、双阶段入库审核 | 本地版完成 |
| P4-02 提案共创 | 缺失项检查、5个相似候选证据、8页结构骨架与京奥品牌 PPTX 初稿 | 标准规则版完成 |
| P3-06 评测基线 | 150 项技术探针与 5 人并发可一键运行并留存报告；业务金标格式已定义 | 技术基线完成，业务标注待办 |
| P0-05 治理规则 | `docs/资料治理规则_v1.md` | 已形成试行版 |

## 实际语料验证

- 授权测试文件：9 份。
- 候选项目：8 个（仅由文件名和清单预填，尚未业务确认）。
- 数据库登记页/逻辑页：249。
- 有文本的页/逻辑页：239，已建立文本块 239 个；但只有 3 份原件仍可复核，当前数字不作为正式抽取率验收结论。
- 受控原件副本：3 份，副本 SHA-256 与入库哈希全部一致。
- 原件失联：6 份，已从正常问答检索中隔离，只保留管理员审核记录。
- 需要 OCR：1 份 2 页扫描 PDF；RapidOCR 本地管线已经实现，但原件失联，恢复同哈希副本后再执行。另已用现场生成的无文字层 PDF 完成自动化验证：本地 OCR 成功识别、按页写入并生成检索分块。
- DOCX 固定分页引用版：1 份已通过 Windows Word 转为 14 页 PDF，首页、中段、末页抽检正常，页码检索分别验证到第 1、8、14 页。
- PPTX 固定分页引用版：1 份 40 页 KPL 执行方案已通过 Windows PowerPoint 转为 PDF；首页、中段、末页视觉抽检正常，原有幻灯片页码与 PDF 页序一致。
- 剩余 1 份 DOCX 原件失联，无法生成引用版。

## 已验证行为

- 历史资料问题可以返回候选证据和页码。
- “最新公司介绍”查询因没有获批 `current` 文档，返回“资料中未找到”。
- L2 账号检索 L3 文档时结果为空，且不返回被拒绝数量。
- 普通员工看不到失联资料数量；创始人可看到失联文档数和隔离提示。
- 原件失联的文本块不进入检索结果，避免产生不可打开、不可核验的引用。
- 当前本地状态 `outbound_enabled=false`，未调用 OriginGame 网关。
- Web → 反向代理 → Agent → 数据库链路可用。
- 前端 lint、生产构建、渲染测试全部通过；Agent 自动化测试 35/35 通过（含扫描 PDF OCR、权限过滤、失联原件隔离、账号隔离、登录限流、备份恢复、运行监控、PPTX 生成、评测运行器、并发验收和 NAS 预检保护）。
- 真实库在线验收通过：9 份登记文档中 3 份原件可复核、6 份失联；`KPL` 历史检索返回 5 个页码级结果，所有返回结果的原件均可用，另有 4 份相关失联文档仅向创始人提示、不进入结果。
- 本地 Web、反向代理和 Agent 健康检查均通过。
- 引用原页预览闭环已通过：检索结果点击后由带身份令牌的 Web 代理读取 PDF，不暴露 NAS 路径；服务端在返回文件前执行权限过滤并记录 `open_source` 审计。真实库 `KPL` 检索第 8 页预览返回 200、`application/pdf`，3 份现存原件均可预览。
- 修复 Vinext 本地图片优化依赖未绑定 `ASSETS.fetch` 导致的整页报错；品牌图片组件显式使用本地原图，Worker 同时增加静态资源绑定和缺少云图片服务时的同源安全回退。浏览器实测登录页与工作台无错误层、无控制台错误，原图返回 200，旧优化地址安全重定向到原图。
- 分层 RAG 代码已完成：当前事实固定走确定性检索；历史案例可走关键词 + Embedding 语义召回，并使用 RRF 融合，不包含 Rerank。网关故障时自动降级到确定性检索并保留引用。
- pgvector 生产列与 SQLite 本地兼容层均已实现；Embedding 模型名写入每条向量记录，文本哈希不变时不重复计费，变更模型可并存后再切换。
- 出网保护已验证：L4 永不建向量、永不参与语义相似度；L3 默认不建向量，关闭开关后会清理既有 L3/L4 向量；索引审计记录模型、分块数与出网字符数，不记录正文。
- Agent 自动化测试覆盖网关响应校验、L3/L4 出网保护、pgvector DDL、混合召回、RRF、语义故障降级、预览权限与审计。
- 本地一致性备份、SHA-256 清单、完成标记、篡改检测和隔离恢复已实现。
- 真实测试库已完成在线备份和恢复演练：1 个用户、8 个项目、9 份文件记录、9 份文档、239 个分块、4 个派生文件记录和 34 条审计记录全部一致；3 份受控原件哈希通过；2 份引用版 PDF 的哈希、页数和可读性通过；备份内 16 个旧会话在恢复时全部注销。恢复发生在临时隔离目录，没有覆盖运行库。
- NAS Compose 已增加每日 `backup` 容器，使用 PostgreSQL 16 `pg_dump/pg_restore`；生产环境必须把 `JINGAO_BACKUP_PATH` 指向另一块介质。
- `docs/Runbook.md` 已覆盖重启、索引重建、备份、隔离恢复、升级与常见故障。
- 本地账号管理已实现：创始人可在 Web 端创建、停用、重新启用、调整角色/部门/密级和重置员工密码；创始人账号不能被员工管理接口修改。
- 普通员工登录流程已修复：只加载其可访问的工作台、项目与检索，不再误调用管理员审核接口；审核和账号管理入口不会出现在普通员工导航中。
- 初始密码和新密码至少 8 位；密码只存 PBKDF2-SHA256 哈希。停用、重置密码和自行改密都会撤销目标账号全部会话，操作写入审计且不记录密码。
- 登录失败限流已完成：默认 15 分钟内最多 5 次失败，锁定 15 分钟；不存在的账号也执行密码哈希校验，失败审计仅记录用户名摘要，避免泄露账号与密码。
- 运行监控已实现：数据库、数据盘、内存、最近完整备份和模型网关分别分级；创始人/资料管理员拥有详细状态页，普通员工无权访问。公开 `/readyz`、`/monitorz` 只返回分级状态，不泄露路径、容量或模型信息。
- NAS Compose 已配置 Agent/Web 容器健康检查；默认磁盘 80% 提醒、90% 严重，内存 80% 提醒、90% 严重，每日备份允许 26 小时窗口。
- 管理员状态页已完成浏览器实测：数据库、数据盘、内存、每日备份、模型网关五张状态卡正常渲染，当前本机状态为“系统运行正常”，页面无控制台错误。
- 可编辑提案 PPTX 已完成：服务端使用经过视觉验证的京奥品牌模板，根据 Brief、缺失项和权限过滤后的候选证据生成 8 页初稿；每页演讲者备注包含 `[Sources]`，下载操作写入审计且不保存 Brief 原文。
- Web 端“提案共创”可直接下载 PPTX；浏览器代理正确转发中文文件名和 PPTX 内容类型。可编辑示例已生成到 `examples/京奥电竞提案初稿示例.pptx`。
- 8 页示例已逐页渲染检查并通过溢出检测；模板文本、色块和图形保持可编辑。Skill 客户端已验证状态查询、历史检索和 PPTX 生成。
- 真实测试库已完成 150 项技术基线：120/120 页码引用通过、15/15 拒答通过、15/15 权限越界探针通过，权限泄露 0，确定性检索 P95 26.08ms；报告保存在本地运行数据目录。
- 管理员可在“系统状态”页面一键重跑评测；页面明确区分“技术基线 150/150”与“业务金标 0/150”，避免把技术探针误当作公司事实验收。
- 真实测试库 5 人并发验收通过：五个合成权限身份完成 10 轮、50 次只读检索，错误 0、权限数量侧信道 0，P95 125.36ms；未创建真实员工账号。
- Agent 自动化测试更新为 39/39 通过，覆盖创始人/员工权限隔离、员工完整登录会话、登录限流、会话撤销、账号审计、隔离恢复、备份时效、公开健康信息脱敏、PPTX 来源备注、评测权限/审计、并发运行器、NAS 预检只读约束，以及审核建议不改原资料、仅创始人可应用/退回、确认后仍为候选和低密级审核队列隔离；前端 lint、生产构建与渲染测试通过。
- 入库审核双阶段闭环已完成：资料管理员/创始人可填写项目、客户、年份、业务线、文档角色、版本、密级和定稿状态并提交建议；提交时 Project/Document 不变。创始人必须输入“确认应用”或“确认退回”，操作全量审计；应用元数据也不会自动晋升 `current`。真实测试库仅完成只读界面验收，未提交或应用任何建议。
- 2026-07-27 增量：NAS `99_AI入库待审核` 自动扫描、120 秒文件稳定窗口、SHA-256 去重、问题登记、手工扫描和系统状态已完成；Agent 测试更新为 45/45。详见 `docs/实施进展_2026-07-27.md`。
- 飞牛 NAS 首次上线只读预检已完成：检查 Linux/x86_64、CPU、内存、Docker/Compose、必填密码、知识目录、备份目录/设备、空间、模型开关、Compose 解析与 3000 端口；不安装、不启动、不改权限、不显示密钥。
- `docs/NAS预检基线_2026-07-26.md` 已区分已确认硬件与待实机证据；当前不会把单盘、无 UPS 或未运行的 NAS 检查误标为发布通过。

## 尚未完成

1. NAS 上的 PostgreSQL/pgvector 实际部署与恢复演练。
2. 恢复扫描件同哈希原件后，完成 RapidOCR 实际回灌；继续补充 PPT 图片文字兜底。
3. 恢复剩余 DOCX 原件并生成引用版 PDF；NAS 上线时评估是否继续采用当前受控 PDF 预览，或切换 kkFileView + 签名 URL。
4. 20 个代表性项目的业务确认与实际入库。
5. 在新密钥通过 NAS Secret 注入后查询网关实际模型目录，锁定 Embedding 模型并建立 L1-L2 首轮索引；完成精确检索与混合检索的对照评测。L3 是否参与索引需创始人另行批准。
6. 确认另外 4 位试点员工的姓名、角色、部门和密级后，由创始人在账号管理界面创建；账号管理、改密、会话撤销和登录限流流程均已完成。
7. 由培训负责人和策划负责人标注 150 道真实业务金标题；技术侧 150 项引用、拒答和权限探针以及本机 5 人并发已经通过。NAS 部署后需在真实硬件上复跑。
8. 确认飞书/微信实际告警接收目标，并在 NAS 上完成 PostgreSQL 实机恢复和容器告警演练；监控接口、管理员状态页、容器健康检查、本地 SQLite 恢复和 Runbook 已完成。
9. 标准 8 页 PPTX 初稿已经可用；超出标准结构的客户正式方案仍需在检索后使用演示文稿能力完成定制叙事、图表与最终视觉润色。

涉及资料真实性、密级调整、`current` 晋升和正式发布的项目，继续等待创始人确认；其余技术工作可继续执行。
