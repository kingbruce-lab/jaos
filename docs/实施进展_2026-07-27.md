# 京奥智库实施进展（2026-07-27）

## 当前结论

NAS 技术部署、局域网 Web、受控原页预览、审核发布、提案 PPTX、每日备份、隔离恢复和运行监控已经形成首版闭环。

当前仍是“合成资料技术试点”，不是“真实公司知识正式发布”。未获创始人确认前，不导入真实资料、不创建真实员工账号、不把候选资料晋升为 current，也不开启 LLM 或 Embedding 出网。

完整逐项状态见 `docs/首版完成度审计_2026-07-27.md`。

## 已完成

- 飞牛 NAS 上运行 7 个容器：PostgreSQL、Agent、Web、ingest、reconcile、backup、Uptime Kuma；
- Web 入口、监控入口、数据库与 Agent 健康检查通过；
- 资料待审核扫描、120 秒稳定窗口、SHA-256 去重和问题清单；
- 元数据建议 → 创始人确认 → 受控发布 → 旧版降级；
- 关键词与向量候选均在 SQL 阶段先执行用户密级、项目密级和知识状态过滤；
- 页码引用、受控 PDF 原页预览、失联原件隔离；
- Brief 到相似案例、提案骨架、风险清单和可编辑 PPTX；
- Grok 主模型、DeepSeek 备用逻辑和 L3/L4 出网拦截代码完成，NAS 开关仍关闭；
- 每日 PostgreSQL 备份封入可用原件，最新恢复基线中 `source_files_missing_at_backup=0`；
- 在独立临时 PostgreSQL 实例和数据卷中完成恢复、数量对账、引用哈希和会话失效验证；
- Uptime Kuma 每 60 秒监控员工入口和 Agent 聚合状态；
- 京奥知识库 Codex Skill 已创建、安装和打包；
- Runbook 和可重复 NAS 部署包已提供。
- NAS 已安装官方 FRPC v0.69.0，配置校验通过，并由 systemd 开机自启和自动重连；
- `knowledge.jingao.club` 已正确解析到公网服务器，FRPC 已指向 NAS Web 的 3000 端口。

## 本轮回归

- Agent：61/61 通过；
- 前端：代码检查、生产构建、服务端渲染测试通过；
- 5 人并发技术基线：50 次请求、0 错误、权限数量侧信道 0；
- 合成语料技术探针：21 项，页码引用 100%、拒答 100%、权限泄露 0；
- 真实业务金标：0/150，等待真实资料与业务负责人标注。

## 当前运行边界

- `JINGAO_LLM_ENABLED=false`
- `JINGAO_LLM_L3_ENABLED=false`
- `JINGAO_EMBEDDING_ENABLED=false`
- `JINGAO_EMBEDDING_L3_ENABLED=false`
- NAS 未注入模型网关 Key；
- 真实公司资料未导入；
- 真实员工账号和权限未创建；
- 候选资料未发布为 current；
- SSH 仅在维护窗口临时开启，任务结束后关闭并移除临时公钥。
- 公网入口尚未验收：NAS 到公网服务器 7001 端口超时，等待服务器安全组或
  主机防火墙放行 NAS 当前公网出口 `106.37.113.25`；最终入口必须走 443
  HTTPS，不直接公开 8000 明文端口。

## 仍需创始人确认

1. 首批 3 个真实资料及其真实性、密级、定稿和 current/历史口径；
2. 5 名员工的登录名、显示名、角色和密级上限；
3. 是否先只开启 L1-L2 LLM，Embedding 暂缓；
4. NAS 外第二备份目标；
5. 飞书/微信告警接收目标。
