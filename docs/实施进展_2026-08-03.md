# 京奥智库实施进展（2026-08-03）

## 当前结论

首份真实公司资料《2026京奥电竞公司介绍》已完成源文件核验、候选入库、42 页逐页解析、OCR 补强、确定性检索实测、创始人元数据确认、独立发布确认和完整备份。资料已于 2026-08-03 11:28:04（北京时间）发布为 `current`，成为公司介绍的当前有效知识来源。

FRP 控制连接、HTTP 虚拟主机代理、Nginx HTTPS 入口和正式域名证书均已完成。`https://knowledge.jingao.club/` 从外网返回 200，证书覆盖 `*.jingao.club` 且校验无错误；HTTP 自动跳转 HTTPS，未登录 API 返回 401，证明公网入口与鉴权边界同时生效。

## 真实资料入库结果

- 文件：`2026京奥电竞公司介绍.pdf`；
- 文件大小：21,202,883 字节；
- SHA-256：`97EF23D597E771F138495C0A4C77A3F7BA3F6BF73833C0A9639C99953A996B6D`；
- PDF 共 42 页，41 页含原生文字层；
- 系统对 6 个低文字量页面执行本地 RapidOCR 补强，42 页均形成可检索文本；
- 不可读页面：0；
- 每页生成 1 个引用分块，共 42 个；
- 引用基准：`hybrid-page`；
- OCR 平均置信度：0.8531；
- 原件在线、哈希核验通过、原页预览可用、候选区无未处理错误；
- 本机逐页渲染和 42 页视觉抽检通过，未发现黑页、裁切、缺页或明显排版损坏。

确定性历史检索已能命中该资料并返回文件名、版本、项目、页码和候选状态。模型与向量出网开关继续关闭，本次解析、检索和质量核验均在 NAS 本地完成。

## 已确认并发布的元数据

创始人于 2026-08-03 明确回复“确认按推荐元数据执行”，系统已通过“提交建议 → 创始人确认应用”两个独立 API 动作完成审计写入：

| 字段 | 已确认值 |
|---|---|
| 项目 | 京奥电竞公司介绍 |
| 年份 | 2026 |
| 业务域 | company |
| 文档角色 | company_profile |
| 版本 | 2026-08 现行版 |
| 密级 | L1（可对外公司介绍） |
| 定稿 | 是 |
| 知识状态 | current |

系统在发布前重新核验阻断项为 0，并按创始人的精确短语 `确认发布为当前版本` 完成独立发布。当前库内没有同谱系的旧现行公司介绍，因此本次替代历史版本数量为 0；原件、页码引用、生效时间和发布审计记录均已保留。

发布后使用 `current + exact` 重新检索，首条结果为 `2026京奥电竞公司介绍.pdf / 2026-08 现行版 / current / L1`，原件不可用数量为 0。创始人随后明确确认登记维护基线，系统已完成独立登记；没有生成复核任务或发布新版本。

## 公司介绍自我进化基线

| 字段 | 当前值 |
|---|---|
| 名称 | 京奥电竞公司介绍 |
| 类型 | company_profile |
| 当前文档 | 2026京奥电竞公司介绍.pdf / 2026-08 现行版 |
| 负责人 | 创始人 |
| 受众 | external（对外） |
| 内容截止日 | 2026-08-03 |
| 复核周期 | 90 天 |
| 下次复核日 | 2026-11-01 |
| 状态 | active |
| 自动发布 | false |
| 当前复核任务 | 0 |

维护流程继承项目层最高密级为 L3，用于限制内部变化清单和披露决策工作台；已发布公司介绍文档本身仍标记为 L1。未来面向对外资料纳入新项目时，仍需创始人逐条输入外部披露确认短语。

## 备份与运行验收

- 入库后备份：`jingao-20260803T031327Z-9cc06319`；
- 元数据确认后备份：`jingao-20260803T032507Z-f65c5f89`；
- 正式发布后备份：`jingao-20260803T032819Z-d9366d1d`；
- 维护基线登记后备份：`jingao-20260803T034853Z-56320a41`；
- 创始人账号重置后备份：`jingao-20260803T041209Z-2d811ac1`；
- 备份状态：`verified`；
- 备份包含 2 份原件、2 个文档、43 个引用分块和 1 个派生记录；
- 最新备份清单、文件哈希和全部可用原件检查通过；
- PostgreSQL、Agent、Web、ingest、reconcile、backup、Uptime Kuma 共 7 个服务运行正常且无异常重启；
- Agent、聚合监控、员工 Web、Uptime Kuma 探针全部通过；
- 磁盘使用约 1%；
- `JINGAO_LLM_ENABLED`、`JINGAO_LLM_L3_ENABLED`、`JINGAO_EMBEDDING_ENABLED`、`JINGAO_EMBEDDING_L3_ENABLED` 均保持 `false`。

## 公网链路状态

已完成：

- NAS 到 `82.156.239.212:7001` 的 FRP 控制连接成功；
- `jingao-frpc.service` 正常运行；
- `jingao-knowledge-web` HTTP 代理注册成功；
- 代理指向 NAS 本机 Web 3000；
- `knowledge.jingao.club` DNS 指向 `82.156.239.212`。
- Nginx 已增加 `knowledge.jingao.club` 独立站点；
- 使用现有 ACME 自动维护的 `*.jingao.club` 通配证书，证书有效至 2026-10-23；
- HTTP 返回 301 到 HTTPS，HTTPS 返回 200，TLS 校验为 0 错误；
- 页面标题、静态资源、CSP、HSTS、防嵌入和权限策略响应头均通过；
- 未登录访问 `/api/kb/v1/status` 返回 401，而不是绕过鉴权；
- 创始人账号已通过公网域名完成真实登录，服务端核验角色为 founder、密级上限为 L4；密码变更同时撤销旧会话；
- Nginx 保留 Host、真实来源和 HTTPS 转发信息，并关闭流式响应缓冲；
- 专用 FRPS 的 7001 继续接受 NAS 控制连接，8000 与 7501 均只绑定 `127.0.0.1`；
- UFW 对 8000 保留显式拒绝规则，公网不能通过原始 FRP HTTP 端口取得应用页面。

调试期间使用过的 FRP 令牌已完成双端轮换，旧令牌失效；新令牌未显示、未进入方案文档，FRPS 与 FRPC 配置权限均为 600，传输用临时文件和旧令牌回滚副本已经删除。仍需单独审计该公网服务器上与本项目无关的历史开放端口；本次没有改动其他业务的端口规则。

## 下一项创始人闸门

公司介绍的发布和维护基线登记均已完成。下一次系统复核日期为 2026-11-01；在此之前可以继续导入已确认的新项目、活动、战队成绩和可披露素材。系统只会形成候选变化清单，任何对外纳入、变更计划、文档发布和基线更新仍分别需要创始人确认。
